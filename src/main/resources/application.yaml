spring:
  datasource:
    url: jdbc:mysql://localhost:3306/joream_project?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true

token:
  secret: joreamjoreamjoreamjoreamjoreamjoreamjoream
  expiration: 60000000