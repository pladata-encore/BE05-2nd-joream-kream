spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jjoream?characterEncoding=UTF-8&serverTimezone=Asia/Seoul
    password: 1234
    username: root

  jackson:
    serialization:
      FAIL_ON_EMPTY_BEANS: false

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        format_sql: true

token:
  secret: joreamjoreamjoreamjoreamjoreamjoreamjoream
  expiration: 600000